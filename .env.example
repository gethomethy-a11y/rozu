# Server-side only. Never prefix with NEXT_PUBLIC_ — that would ship it to the browser.
# Set the real value in Vercel > rozu > Settings > Environment Variables.
# If unset, /api/generate serves the deterministic fallback routine instead of erroring.
ANTHROPIC_API_KEY=
